# 敏华 AI 绘画系统：模型升级/迁移技术变更文档（预览版 ➡️ 正式版）

为便于开发团队以及周边系统（例如：财务对账系统、看板系统、数仓及报表、数据审计、计费系统等）进行对应接口和数据模型的同步更新，特编制此模型迁移与升级指导技术文档。

---

## 一、 控制台与网关模型名称映射
在升级到**正式版（Production/Release Ver.）**后，API 请求中的 `model` 字段及其底层的路由逻辑对应关系调整如下：

| 原预览版模型 (Preview Model) | 现正式版模型 (Release Model) | 系统前台显示名称 | 扣点计费档位类型 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| `gemini-3.1-flash-image-preview` | `gemini-3.1-flash-image` | **新闪电·正式版** | 2K 锚点计费 (标准版档位) | 适用于快节奏、高频率的普通家具/沙发场景渲染 |
| `gemini-3-pro-image-preview` <br> `google/gemini-3-pro-image-preview` | `gemini-3-pro-image` <br> `google/gemini-3-pro-image` | **新旗舰·正式版** | 4K 锚点计费 (旗舰版档位) | 适用于超高精细、光影极致、多重材质要求的专利渲染与空间重绘 |

---

## 二、 数据库日志及系统看板数据标示规范 (Data Log Tag Markers)
为向后兼容并便于数仓/数据看板系统通过模糊查询或精准定位统计各版本生图能耗，后台接口在将生成的生图数据存入 `usage_logs` 表时，对 `model` 字段会严格按照下述带有**数据日志标记**的特殊前缀进行统一存储转换：

```
DB 存储字段 `usage_logs.model` 转换规则：
[展示标识] 实际调用模型 [分流服务及耗时标识]
```

### 1. 核心模型数据库存储标记对应表 (必读)

| 物理调用模型 `model` | 数据库 `usage_logs.model` 保存形式 (带有日志标记区分) | 适用场景 / 对应前台版本 |
| :--- | :--- | :--- |
| **`gemini-3.1-flash-image`** | `[⚡新闪电·正式版] gemini-3.1-flash-image` | 标准生图正式版通道 |
| `gemini-3.1-flash-image-preview` | `[⚡新闪电·预览版] gemini-3.1-flash-image-preview` | 历史/预览版遗留通道 |
| **`gemini-3-pro-image`** | `[💎新旗舰·正式版] gemini-3-pro-image` | 旗舰生图正式版通道 |
| `gemini-3-pro-image-preview` | `[💎新旗舰·预览版] gemini-3-pro-image-preview` | 历史/预览版遗留通道 |
| `*2.5*` 或 `*极速*` | `[🚀极速版·v2.5] {model}` | 极速体验测试通道 |
| `gpt-image-2*` | `[渠道·2.0] {model}` | 第三方联合测试通道 (2.0) |
| `gpt-image-1.5*` | `[渠道·1.5] {model}` | 第三方联合测试通道 (1.5) |
| `gpt-image-1*` | `[渠道·1.0] {model}` | 第三方联合测试通道 (1.0) |

> 💡 **注：后置分流标识**  
> 若底层发生流控升级或网关备用替换，日志中后置拼接部分会自动变化。如：
> - `[⚡新闪电·正式版] gemini-3.1-flash-image - V (耗时 2.5s)` (通过 VectorEngine 执行)
> - `[💎新旗舰·正式版] gemini-3-pro-image - V转R (耗时 5.1s)` (由 VectorEngine 降级/重试转至 RouterHub 执行)

---

## 三、 生图扣点/计费费率对应表 (Image Points Billing Rules)
为确保第三方财务与计费系统对账正常，各对应模型在不同产出分辨率（`resolution`）下的耗扣点数标准如下（1 元人民币 💰 理论等价于 10,000 点数）：

### 计费标准矩阵 (单位：点数)

| 模型类别 / 费率档位 | 1K 分辨率扣点 (`resolution="1K"`) | 2K 分辨率扣点 (`resolution="2K"`) | 4K 分辨率扣点 (`resolution="4K"`) | 默认或缺省扣点 (未指定分辨率) |
| :--- | :--- | :--- | :--- | :--- |
| **标准版 / 新闪电** (`3.1`) | **1,100 点** | **2,200 点** *(锚点)* | **4,400 点** | **2,200 点** *(以 2K 锚点扣减)* |
| **旗舰版 / 新旗舰** (`3.0`/`pro`) | **1,250 点** | **2,500 点** | **5,000 点** *(锚点)* | **5,000 点** *(以 4K 锚点扣减)* |
| **极速版** `v2.5` | **200 点** | **200 点** | **200 点** | **200 点** |
| **渠道通道** (`gpt-image`) | **1,500 点** | **3,000 点** | **6,000 点** | **3,000 点** |

---

## 四、 后台逻辑及异常拦截标记变更
1. **生图未成功（敏感词拦截/网络丢包/上游出错）等异常扣费计分化：**
   - 传统预览版本在遇到生图失败时会直接中途终止不生成记录。
   - 正式版引入了优雅拦截：若上游接口未成功返回有效的图像数据，则扣费系统会将该次调用记作 **0 扣点**，并在 `usage_logs.model` 保存标签：
     `[生图未成功 - {错误原因}] (耗时 {耗时}s)`。该记录旨在帮助运维团队进行故障原因和调用失败率审计，而无需对用户产生余额费用损耗。

2. **多通道渲染长图尺寸自动重设保障：**
   - 正式版支持带有自定义宽高比例模式。如果在 `model_res` 被设定为特定尺寸比例（如 `3974:1533`），正式版会在生图结束后，使用服务器侧的高性能 `sharp` 模块进行 **Cover Resizing**，确保最终生成的图片像素分毫不差地吻合设定，提升了 CAD 物理排布计算的精度。

---

## 五、 与第三方系统对接与 SQL 提取范例

报表或周边系统在进行统计时，可以通过以下 SQL 逻辑完美识别正式版与预览版的数据日志记录：

```sql
-- 1. 统计正式版渠道的总消耗点数与调用次数
SELECT 
    CASE 
        WHEN model LIKE '[⚡新闪电·正式版]%' THEN '新闪电·正式版'
        WHEN model LIKE '[💎新旗舰·正式版]%' THEN '新旗舰·正式版'
        ELSE '其他通道'
    END AS model_category,
    COUNT(*) as total_calls,
    SUM(tokens_used) as total_points_deducted
FROM usage_logs
WHERE model LIKE '%正式版%'
GROUP BY 1;

-- 2. 筛查生图失败的运维审计日志
SELECT 
    created_at,
    user_id,
    model as fail_details
FROM usage_logs
WHERE model LIKE '[生图未成功 - %';
```

---
*敏华 AI 绘画系统开发攻坚组 ｜ 运维保障团队 发布 (2026-06)*
