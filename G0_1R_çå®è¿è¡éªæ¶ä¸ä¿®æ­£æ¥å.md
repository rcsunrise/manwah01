# G0-1R｜智能体连续对话真实运行验收与修正报告

日期：2026-08-08  
基线：C4B-2 11/11、C4B-3 18/18、G0-1 31/31 上传代码

## 阶段结论

本轮仅执行 G0-1R，没有进入 G0-2、G0-3、C4B-4、生图 Provider 或任务队列开发。

- G0-1 静态契约：31/31 通过。
- G0-1R 真实 HTTP/SSE 测试 Adapter：30/30 通过。
- C4B-2 回归：11/11 通过。
- C4B-3 回归：18/18 通过。
- Vitest：37 个测试文件、204 项测试全部通过。
- TypeScript / lint：通过。
- 生产构建：通过；存在既有的大包体积警告，不阻塞本阶段。

自动化测试使用显式、密钥隔离的文本 Mock Stream 和测试文件存储。由于当前执行环境没有 Supabase、Gemini/文本模型凭证，因此本报告不把 30/30 描述为真实 Supabase RLS 或真实文本 Provider 验证。

## A. 真实对话请求与响应证据

测试通过真实启动的 Express 服务发起 HTTP 请求，而非读取源码字符串：

1. `POST /api/agent/conversations` 创建会话。
2. `POST /api/agent/conversations/:id/messages` 保存 User Message 并返回 SSE。
3. `GET /api/agent/conversations/:id/messages` 重新读取消息事实。
4. 停止服务并重新启动，再次读取同一会话。

单次成功响应收到 41 个 `response.delta`，合并正文与 `response.completed.message.content.text` 完全一致。

## B. SSE 实际事件顺序

成功请求顺序：

```text
conversation.created
message.user.saved
response.started
response.delta × 41
response.completed × 1
```

失败请求以 `response.failed` 结束。客户端断开时不发送或保存伪 `completed`。

前端 SSE 解析器已改为按完整事件块解析，能够处理 `event:` 与 `data:` 被网络分片拆开的情况；如果流结束前没有终态事件，前端明确报告 `STREAM_INTERRUPTED`。

## C. Conversation 与 Message 持久化

- User Message 在模型调用前保存。
- Assistant Message 先以 `streaming` 保存，再更新为 `completed` 或 `failed`。
- User 或 Assistant 初始状态保存失败时返回 `MESSAGE_PERSISTENCE_ERROR`，不先伪报成功。
- Provider 失败保存 `failed` 与明确错误码。
- Provider Response ID 仅在实际获得时保存；不再伪造 Gemini Response ID。
- 生产模式只使用 Supabase 作为对话事实源；文件存储只在显式 `AGENT_CHAT_TEST_MODE=true` 时启用。

## D. 刷新与服务重启恢复

测试重新请求会话列表和消息接口，验证不依赖 React 内存状态。服务进程停止并重新启动后，7 条测试消息及其 `completed`、`failed/STREAM_INTERRUPTED` 状态均被恢复。

## E. 停止生成

慢速流收到至少两个 delta 后主动断开：

- Assistant Message 最终状态为 `failed`；
- `error_code` 为 `STREAM_INTERRUPTED`；
- 已返回的局部正文被保存；
- 不创建 `completed` 消息。

## F. Retry 幂等性

测试先模拟 `PROVIDER_TIMEOUT`，再调用 `/retry`：

- 重试成功产生新的 Assistant Message；
- User Message 数量不增加；
- 新 Assistant Message 复用原失败响应对应的 `parent_message_id`；
- 只有最新失败的 Assistant Response 可以重试。

## G. 用户、Canvas 与场景隔离

- 未登录请求返回 `401 / AGENT_AUTH_ERROR`。
- 已删除无凭证时自动赋予固定用户的逻辑。
- 已删除固定用户绕过 Conversation 所有权的逻辑。
- 创建 Conversation 前校验 Project、Canvas、用户归属。
- 用户 B 读取用户 A 的 Conversation 返回 `403 / CONVERSATION_FORBIDDEN`。
- 无效或跨 Canvas 的版本 ID 返回 `INVALID_AGENT_CONTEXT`。

## H. 实际 context_snapshot 字段

```json
{
  "projectId": "project-g0-a",
  "canvasId": "canvas-g0-a",
  "activeSceneKey": "scene-01",
  "selectedNodeIds": ["scene-node-01"],
  "productDnaVersionId": "dna-version-a",
  "assetVersionId": "asset-version-a",
  "copyVersionId": "copy-version-a",
  "typographySpecId": "typography-spec-a"
}
```

历史快照在后续场景变化后保持不变。生产路径会逐项验证 DNA、Asset、Copy、Typography 版本与当前 Project/Canvas 的归属链。

## I. 零副作用运行计数

```json
{
  "canvasNodeWrites": 0,
  "copyVersionWrites": 0,
  "typographySpecWrites": 0,
  "imageProviderCalls": 0,
  "billingCalls": 0,
  "toolCalls": 0
}
```

本阶段没有画布节点写入、Copy Version 写入、Typography Spec 写入、图片 Provider、计费或工具调用。

## J. 隐藏推理与敏感字段

SSE 只允许以下白名单事件：

- `conversation.created`
- `message.user.saved`
- `response.started`
- `response.delta`
- `response.completed`
- `response.failed`

测试确认流中没有系统指令全文、Authorization、API Key、隐藏推理或 Chain of Thought。数据库不保存隐藏推理。

## K. RLS 修正

- Conversation 写入策略要求 `auth.uid() = user_id`，并要求 Canvas 的 `id`、`project_id`、`created_by` 与当前用户一致。
- Message 写入策略除要求 `auth.uid() = user_id` 外，还要求父 Conversation 属于当前用户。
- 旧的宽松 `FOR ALL` 策略先删除再以带 `WITH CHECK` 的策略重建，避免策略 OR 组合留下旁路。

必须在正式 Supabase 项目执行更新后的 `supabase_agent_chat_schema.sql` 后，才能验证真实 RLS。

## L. 新增与修改文件

新增：

- `server/agentChat/runtimeTestSupport.ts`
- `scripts/verify_g0_1_runtime.cjs`
- `scripts/run_c4b_http_regressions.cjs`
- `tests/agent_chat_runtime_support.test.ts`
- `tests/agent_chat_service_stream.test.ts`
- 本报告

修改：

- `server/routes/agentConversationRoutes.ts`
- `src/services/agentChatService.ts`
- `supabase_agent_chat_schema.sql`
- `server.ts`
- `src/lib/supabase.ts`
- `scripts/verify_c4b3.cjs`
- `package.json`
- `package-lock.json`

`src/lib/supabase.ts` 的 Mock 查询语义同步修正：带过滤条件的 `maybeSingle()` 无匹配时返回 `null`，并正确支持多次 `.eq()` 与 `insert().select()`，因此 C4B-2 的 404、不变性和并发回归重新稳定通过。

## M. 最终命令结果

| 命令 | 结果 |
|---|---:|
| `node scripts/run_c4b_http_regressions.cjs` | C4B-2 11/11；C4B-3 18/18 |
| `node scripts/verify_g0_1.cjs` | 31/31 |
| `node scripts/verify_g0_1_runtime.cjs` | 30/30 |
| `npm run lint` | 通过 |
| `npx tsc --noEmit` | 通过 |
| `npx vitest run` | 37 files / 204 tests 通过 |
| `npm run build` | 通过，只有非阻塞 chunk size warning |

## N. G0-1 正式封板与 G0-2 准入

代码级、真实 HTTP/SSE、测试持久化、断流、重试、进程重启和应用层跨用户隔离已经通过。

正式进入 G0-2 前还需要在 AI Studio 部署环境完成三项外部冒烟验收：

1. 执行更新后的 Supabase Schema，并用两个真实登录用户验证 RLS。
2. 使用真实文本 Provider 完成至少一轮非付费图片的流式对话。
3. 在 AgentPanel 完成发送、停止、刷新恢复与重试的浏览器操作验收。

三项通过后即可把 G0-1 正式封板并进入 G0-2。当前代码没有进入或预实现 G0-2。
